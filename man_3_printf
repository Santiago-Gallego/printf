.TH _printf "17 Mar 2020" "_printf man page"
.SH NAME
.B _printf
- prints the character(s)
.SH SYNOPSIS
#include "holberton.h"
int _printf(const char *format, ...);
.SH DESCRIPTION
The printf() function prints output of a string, character, or integer. The printf function is able to print a string in quotes.
.SH OPTIONS
If the string has a '%' it looks to the following character.
If it has a 'c' it will print a char variable.
if it is a 's' it will print a string variable.
if it is a 'i' it will print an integer value.
if it is a 'd' it will print a integer.
if it is a '%' it will print a modulo.
if it is a 'b' it will print a unsigned int converted to binary.
.SH RETURN
The function will returen the number of characters printed. If unsuccessful the function will return null.
.SH AUTHOR
Santiago Gallego, Juan David Suarez, Holberton School Cohort 11 BOG
